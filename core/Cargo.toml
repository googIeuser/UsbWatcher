[package]
name = "usb_watcher_core"
version = "0.1.0"
edition = "2021"
rust-version = "1.79"
description = "Local USB enumeration core for USB Watcher"
license = "MIT"
publish = false

[dependencies]
nusb = "0.2.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
